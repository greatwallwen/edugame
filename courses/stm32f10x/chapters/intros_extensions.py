"""
章节引入页和拓展页定义

本模块集中管理所有章节的 intro 和 extension 页面，避免在 manifest_builder.py 中堆积重复代码。
每个章节包含：
- intro_chN: 章节引入页（objectives + tasks + prereqs）
- ext_chN: 章节拓展页（mistakes + tips + cards）

依赖：
- factories.build_intro_page
- factories.build_extension_page
"""

from manifest.factories import build_intro_page, build_extension_page


# ── 第3章：GPIO应用 ────────────────────────────────────────────────
intro_ch3 = build_intro_page("ch3-intro", "第3章", "GPIO应用",
    objectives=[("理解GPIO推挽/开漏/输入三类工作模式", "理解"),
                ("搭建LED限流驱动电路并实现闪烁/流水/呼吸灯", "应用"),
                ("实现按键轮询扫描与外部中断两种方案", "应用"),
                ("掌握NVIC中断优先级配置原则", "理解")],
    intro_text=("在嵌入式开发中，GPIO（通用输入输出）是最基础的外设接口。"
                "本章通过LED和按键两个经典实验，掌握GPIO的输出驱动与输入检测，"
                "并深入理解中断机制——这是后续所有中断类外设（UART/Timer等）的基础。"),
    tasks=[("3.1", "LED灯闪烁与流水灯", "推挽输出·限流电阻·TogglePin", "45min"),
           ("3.2", "按键扫描与外部中断", "输入模式·消抖·EXTI·NVIC", "45min"),
           ("3.3", "常见问题与知识拓展", "调试技巧·进阶应用", "20min")],
    prereqs=["第1章：单片机基本概念", "第2章：CubeIDE开发环境"])

ext_ch3 = build_extension_page("ch3-ext", "第3章", "GPIO应用",
    mistakes=[("未使能GPIO时钟", "__HAL_RCC_GPIOA_CLK_ENABLE()必须在Init之前调用，否则GPIO操作无效"),
              ("中断回调中调用HAL_Delay", "HAL_Delay依赖SysTick，在高优先级中断中会死锁；消抖改在主循环处理"),
              ("LED不亮排查顺序", "先测电压：引脚输出是否3.3V？再测电阻：阻值对吗？再看LED极性"),
              ("浮空输入读值抖动", "按键引脚必须配置上拉(PULLUP)，否则未按下时读值不稳定")],
    tips=[("同时翻转多引脚", "HAL_GPIO_TogglePin(GPIOA, GPIO_PIN_5|GPIO_PIN_6) 用位OR一次操作"),
          ("BSRR原子操作", "GPIOA->BSRR = GPIO_PIN_5 置高，>>16置低，中断安全无竞态"),
          ("用SysTick实现软件定时", "HAL_GetTick()返回ms计数，(now-last)>=500实现非阻塞延时")],
    cards=[("GPIO推挽与开漏的本质区别？", "推挽：P管+N管双向驱动，能主动输出高低；开漏：只有N管，高电平靠外部上拉"),
           ("EXTI中断不触发怎么排查？", "检查：①CubeMX配置了触发方式？②NVIC全局中断使能？③回调函数名拼写正确？"),
           ("按键消抖最简单的方法？", "检测到低电平后HAL_Delay(20ms)再次确认，或在主循环用时间戳判断间隔")])


# ── 第4章：定时器应用 ──────────────────────────────────────────────
intro_ch4 = build_intro_page("ch4-intro", "第4章", "定时器应用",
    objectives=[("理解PSC/ARR/CNT三个寄存器的作用", "理解"),
                ("计算定时器溢出周期", "应用"),
                ("实现定时器中断回调的周期性任务", "应用"),
                ("掌握数码管动态扫描原理", "理解")],
    intro_text=("定时器是嵌入式系统中最核心的外设之一。精确计时、PWM生成、输入捕获、"
                "编码器接口——都依赖定时器。本章重点掌握通用定时器(TIM2~5)的基础应用，"
                "为后续PWM和脉冲捕获奠定基础。"),
    tasks=[("4.1", "定时器原理及中断应用", "PSC/ARR·溢出中断·数码管", "60min"),
           ("4.2", "常见问题与知识拓展", "计算验证·多路定时·进阶用法", "20min")],
    prereqs=["第2章：CubeIDE与HAL库", "第3章：GPIO配置基础"])

ext_ch4 = build_extension_page("ch4-ext", "第4章", "定时器应用",
    mistakes=[("APB时钟倍频误算", "TIM2~5在APB1，但APB1分频≠1时定时器时钟=APB1×2=72MHz，不是36MHz"),
              ("忘记判断htim->Instance", "多个定时器共用一个回调，必须if(htim->Instance==TIM2)区分"),
              ("ARR设置为0", "ARR=0时立刻溢出，效果异常；最小有意义值为1")],
    tips=[("软件多路定时", "用1ms基础定时器+计数变量，cnt500=500ms任务，cnt1000=1s任务，一个定时器搞定多个周期"),
          ("定时器精度验证", "用逻辑分析仪或示波器测量GPIO翻转周期，与计算值对比验证"),
          ("不用中断的软件延时对比", "HAL_Delay阻塞CPU；定时器中断不阻塞，推荐用于实际项目")],
    cards=[("PSC=71, ARR=999, 72MHz时钟，定时周期？", "计数频率=72M/72=1MHz；周期=(999+1)/1MHz=1ms"),
           ("TIM1和TIM2哪个在APB2？", "TIM1在APB2(72MHz)；TIM2~7在APB1(36MHz，但实际定时器时钟72MHz)"),
           ("定时器中断为何无响应？", "检查：①HAL_TIM_Base_Start_IT()已调用？②NVIC中断使能？③回调函数实现了吗？")])


# ── 第5章：PWM输出 ─────────────────────────────────────────────────
intro_ch5 = build_intro_page("ch5-intro", "第5章", "PWM输出",
    objectives=[("理解PWM占空比与等效电压的关系", "理解"),
                ("配置TIM2通道生成PWM波形", "应用"),
                ("实现LED呼吸灯效果", "应用"),
                ("掌握舵机PWM控制时序参数", "理解")],
    intro_text=("PWM（脉冲宽度调制）通过改变数字信号的占空比来模拟模拟量输出，"
                "是LED调光、电机调速、舵机控制的核心技术。本章基于第4章定时器知识，"
                "学习定时器的输出比较功能。"),
    tasks=[("5.1", "PWM波形输出与呼吸灯", "占空比·TIM通道·呼吸灯", "60min"),
           ("5.2", "常见问题与知识拓展", "舵机控制·频率选择", "20min")],
    prereqs=["第4章：定时器原理与中断"])

ext_ch5 = build_extension_page("ch5-ext", "第5章", "PWM输出",
    mistakes=[("PA0未配置为复用推挽", "GPIO模式必须是AF_PP，不能是普通OUTPUT_PP，否则PWM无输出"),
              ("舵机抖动", "舵机50Hz=20ms周期，PSC/ARR计算错误导致频率偏差，用示波器验证脉宽"),
              ("呼吸灯不平滑", "线性递增产生视觉不均匀，改用gamma校正曲线或sin函数平方曲线")],
    tips=[("动态修改占空比", "__HAL_TIM_SET_COMPARE(&htim2, TIM_CHANNEL_1, ccr)是唯一正确方式，勿直接改寄存器"),
          ("多通道PWM", "同一定时器4个通道可独立设置占空比，驱动4个LED或4路电机"),
          ("PWM频率与噪音", "无刷电机用20kHz以上可消除人耳可听噪音（>20kHz为超声波范围）")],
    cards=[("CCR=500, ARR=999, 占空比？", "占空比=500/(999+1)×100%=50%，等效电压=3.3V×50%=1.65V"),
           ("舵机需要什么频率的PWM？", "50Hz（周期20ms），脉宽0.5ms→0°，1.5ms→90°，2.5ms→180°"),
           ("如何验证PWM输出正常？", "示波器测PA0：频率对吗？占空比对吗？或用LED亮度判断")])


# ── 第6章：串口通信 ────────────────────────────────────────────────
intro_ch6 = build_intro_page("ch6-intro", "第6章", "串口通信（UART）",
    objectives=[("理解UART通信协议与波特率概念", "理解"),
                ("配置USART1实现printf输出", "应用"),
                ("使用中断方式接收命令", "应用"),
                ("掌握DMA+IDLE中断接收不定长数据", "应用")],
    intro_text=("UART（串口）是最常用的通信接口，用于MCU与PC、传感器、模块之间的数据交换。"
                "本章掌握HAL_UART_Transmit/Receive三种模式（轮询/中断/DMA），"
                "并实现常见的printf重定向和命令解析。"),
    tasks=[("6.1", "串口基本收发与printf重定向", "波特率·printf·接收中断", "60min"),
           ("6.2", "常见问题与知识拓展", "DMA·IDLE·环形队列", "20min")],
    prereqs=["第2章：CubeIDE环境"])

ext_ch6 = build_extension_page("ch6-ext", "第6章", "串口通信（UART）",
    mistakes=[("printf不输出", "必须在main.c中重写fputc，且CubeMX中USART1 Mode=Asynchronous已使能"),
              ("接收中断只触发一次", "HAL_UART_Receive_IT在回调末尾必须再次调用，否则只响应一次"),
              ("乱码排查", "检查：①双方波特率是否一致？②UART配置8N1？③USB转串口驱动装对了吗？")],
    tips=[("环形队列收数据", "中断将字节写入ring_buf，主循环从中读取并解析帧；避免主循环处理与中断竞争"),
          ("DMA接收不定长", "UART_RxCpltCallback+IDLE中断组合，实现不定长帧自动接收"),
          ("串口调试工具推荐", "SSCOM/MobaXterm/minicom；发送时注意换行格式（\\r\\n vs \\n）")],
    cards=[("UART 115200/8N1 表示什么？", "115200bps波特率，8位数据位，无奇偶校验，1位停止位"),
           ("printf重定向原理？", "重写fputc函数，将putchar的字节转发到HAL_UART_Transmit，即可用printf输出到串口"),
           ("如何接收不定长命令？", "使用HAL_UARTEx_ReceiveToIdle_IT，IDLE线路空闲时自动产生中断")])


# ── 第7章：ADC模数转换 ────────────────────────────────────────────
intro_ch7 = build_intro_page("ch7-intro", "第7章", "ADC模数转换",
    objectives=[("理解ADC采样定理与12位分辨率", "理解"),
                ("计算ADC原始值对应的实际电压", "应用"),
                ("配置STM32 ADC单通道轮询采样", "应用"),
                ("设计光敏电阻分压电路并读取光照强度", "应用")],
    intro_text=("ADC（模数转换）是连接物理世界与数字世界的桥梁。温度、光照、声音、"
                "气压等连续信号，都需要通过ADC转换为MCU可处理的数字值。"
                "本章以光敏电阻为例，掌握ADC的配置与读取方法。"),
    tasks=[("7.1", "ADC采样与光照检测", "分辨率·采样时间·光敏电阻", "60min"),
           ("7.2", "常见问题与知识拓展", "多通道·DMA·滤波", "20min")],
    prereqs=["第2章：GPIO模拟输入配置", "第4章：定时器（可选，用于触发ADC）"])

ext_ch7 = build_extension_page("ch7-ext", "第7章", "ADC模数转换",
    mistakes=[("ADC引脚未配置为模拟模式", "PA0用于ADC时，GPIO模式必须是GPIO_MODE_ANALOG，否则精度极差"),
              ("采样时间太短导致读值不稳", "高阻抗传感器（如光敏电阻）需要239.5个ADC时钟周期，不能设1.5"),
              ("读值始终为4095或0", "检查VREF+引脚是否连接3.3V，检查信号是否超出ADC量程")],
    tips=[("多通道扫描+DMA", "ADC1扫描模式+DMA循环，自动轮询多路传感器，CPU不参与"),
          ("滑动平均滤波", "取8次采样平均，消除高频噪声；不需要float，整数运算即可"),
          ("内部温度传感器", "ADC1_IN16是内部温度传感器，Temp=((V-0.76)/0.0025)+25，可免硬件测温")],
    cards=[("12位ADC读到2048，VREF=3.3V，电压？", "2048×3.3/4096=1.65V（满量程50%）"),
           ("为什么要用分压电路？", "光敏电阻是纯阻性器件，不能直接接ADC；分压后电压随光照变化"),
           ("ADC触发方式有哪些？", "软件触发（Start+Poll）、定时器触发（定时采样）、外部触发")])


# ── 第8章：DAC数模转换 ────────────────────────────────────────────
intro_ch8 = build_intro_page("ch8-intro", "第8章", "DAC数模转换",
    objectives=[("理解DAC与ADC的互逆关系", "理解"),
                ("配置STM32 DAC输出固定电压", "应用"),
                ("使用DMA+定时器生成周期波形", "应用"),
                ("计算正弦波表并验证输出频率", "应用")],
    intro_text=("DAC（数模转换）与ADC功能相反，将数字值转换为模拟电压输出，"
                "是信号发生器、音频播放、电机驱动参考电压等应用的核心。"
                "STM32F103集成2路12位DAC，本章掌握其基础应用与波形生成。"),
    tasks=[("8.1", "DAC输出与波形生成", "固定输出·正弦波·三角波·DMA", "60min"),
           ("8.2", "常见问题与知识拓展", "波形频率计算·调试方法", "20min")],
    prereqs=["第4章：定时器（用于触发DAC）", "第3章：GPIO模拟输出"])

ext_ch8 = build_extension_page("ch8-ext", "第8章", "DAC数模转换",
    mistakes=[("输出范围不是0~3.3V", "DAC有约0.2V死区，实际输出约0.2~3.1V；需要输出到0V需外部运放"),
              ("启用Output Buffer后驱动能力不足", "Buffer使能后可驱动小负载（>10kΩ）；低阻抗负载需外部运放缓冲"),
              ("波形频率计算错误", "波形频率=触发频率/波形表点数；256点表，触发1MHz时，波形频率=1M/256≈3.9kHz")],
    tips=[("用串口发送命令切换波形", "接收命令后更新DMA指向不同波形表，实现信号发生器效果"),
          ("示波器验证", "PA4接示波器探头，观察波形形状和频率，调整ARR值改变频率"),
          ("DAC+ADC联调", "DAC输出接ADC输入，采集后对比，验证两者精度是否一致")],
    cards=[("DAC_Value=2048, VREF=3.3V, 输出电压？", "2048×3.3/4096=1.65V"),
           ("正弦波表中心值是多少？", "中心=2048（对应1.65V），幅值=±2047（最大4095，最小1）"),
           ("如何提高DAC波形频率？", "减小ARR值（提高触发频率）或减少波形表点数")])


# ── 第9章：环境监测系统 ───────────────────────────────────────────
intro_ch9 = build_intro_page("ch9-intro", "第9章", "环境监测系统",
    objectives=[("掌握MQ-2烟雾传感器的ADC采样方案", "应用"),
                ("理解I2C总线协议并操作BH1750光照传感器", "应用"),
                ("通过HDC1080读取温湿度数据", "应用"),
                ("设计多传感器分时采集架构", "综合")],
    intro_text=("环境监测是物联网的核心应用场景。本章整合ADC（第7章）和I2C通信，"
                "融合三种不同接口的传感器，实现烟雾、光照、温湿度的同时监测，"
                "是对前序知识的综合应用。"),
    tasks=[("9.1", "多传感器环境监测实验", "MQ-2·BH1750·HDC1080", "90min"),
           ("9.2", "系统集成与调试", "分时采集·串口上报·LCD显示", "30min")],
    prereqs=["第7章：ADC采样", "第2章：GPIO与I2C配置"])

ext_ch9 = build_extension_page("ch9-ext", "第9章", "环境监测系统",
    mistakes=[("I2C地址左移错误", "HAL要求地址左移1位：0x23<<1=0x46传给Transmit/Receive，初学者常忘"),
              ("BH1750未等待转换时间", "H-Resolution模式需120ms，立即读取得到上次数据；加HAL_Delay(180)"),
              ("MQ-2预热不足", "首次上电需>24小时，日常至少5分钟预热；预热不足读值偏低不准")],
    tips=[("传感器故障快速定位", "逐一单独测试：先单独测MQ-2，再测BH1750，最后测HDC1080，排除干扰"),
          ("数据上报格式", "用JSON格式：{\"temp\":25.3,\"humi\":60,\"lux\":300,\"smoke\":120} 方便上位机解析"),
          ("定时采集避免阻塞", "用1s定时器中断触发采集，不用while(1)+Delay，保持主循环响应")],
    cards=[("BH1750的I2C地址是多少？", "ADDR引脚接GND时0x23，接VCC时0x5C；HAL传参需左移：0x23<<1"),
           ("HDC1080温度数据如何换算？", "raw/65536×165-40，例：raw=39321 → 39321/65536×165-40≈58.9°C"),
           ("如何处理传感器读取超时？", "HAL_I2C_Master_Transmit返回HAL_TIMEOUT时，重新初始化I2C或复位传感器")])


# ── 第10章：无人停车场系统 ────────────────────────────────────────
intro_ch10 = build_intro_page("ch10-intro", "第10章", "无人停车场系统",
    objectives=[("理解SPI协议四线结构与主从通信模式", "理解"),
                ("使用HC-SR04超声波传感器实现距离测量", "应用"),
                ("通过MFRC522 NFC模块读取IC卡UID", "应用"),
                ("设计有限状态机实现停车场控制逻辑", "综合")],
    intro_text=("无人停车场是嵌入式系统综合应用的典型案例，涵盖传感器检测、"
                "身份认证、执行器控制和状态管理。本章引入SPI通信协议（用于NFC模块），"
                "综合运用超声波测距、舵机控制和有限状态机设计。"),
    tasks=[("10.1", "HC-SR04超声波测距", "TRIG/ECHO时序·距离计算", "45min"),
           ("10.2", "NFC模块与状态机控制", "SPI·MFRC522·状态机", "60min"),
           ("10.3", "系统集成与调试", "联调·异常处理·优化", "30min")],
    prereqs=["第3章：GPIO基础", "第5章：PWM舵机控制", "第4章：定时器"])

ext_ch10 = build_extension_page("ch10-ext", "第10章", "无人停车场系统",
    mistakes=[("HC-SR04 ECHO 5V损坏STM32", "ECHO输出5V，STM32是3.3V！必须分压（1kΩ+2kΩ）或用电平转换芯片"),
              ("SPI速率过高导致通信失败", "MFRC522最高10MHz，初学建议1~4MHz，Speed=Prescaler/16=4.5MHz"),
              ("状态机忘记超时保护", "NFC_WAIT状态必须有超时（如10s），否则等不到刷卡时系统卡死")],
    tips=[("DWT精确微秒延时", "使用DWT->CYCCNT计数实现μs级delay，比TIM中断开销更小"),
          ("NFC卡UID存储", "将授权卡UID存入Flash，开机读取白名单；支持多卡管理"),
          ("舵机防卡死保护", "设置舵机最大运行时间（如3s），超时强制复位，避免舵机堵转烧毁")],
    cards=[("HC-SR04测距公式？", "距离(cm)=ECHO高电平时间(μs)×0.034/2"),
           ("SPI和I2C最大区别？", "SPI：4线，高速，全双工，无地址；I2C：2线，较慢，半双工，有7位地址"),
           ("状态机设计的核心原则？", "每个状态只做一件事，转换条件明确；避免在状态内部阻塞等待")])


# ── 第11章：运动手环设计 ───────────────────────────────────────────
intro_ch11 = build_intro_page("ch11-intro", "第11章", "运动手环设计",
    objectives=[("理解LSM6DS3六轴IMU的寄存器配置", "应用"),
                ("理解PPG光电容积传感原理", "理解"),
                ("实现基于峰值检测的计步算法", "应用"),
                ("掌握STM32低功耗Stop模式设计", "应用")],
    intro_text=("运动手环是可穿戴设备的代表性产品，融合了IMU传感、生理信号采集、"
                "算法处理和低功耗设计。本章以LSM6DS3和PPG模块为核心，"
                "实现计步、卡路里计算和心率提取。"),
    tasks=[("11.1", "IMU与PPG数据采集", "LSM6DS3·I2C配置·PPG原理", "60min"),
           ("11.2", "计步算法与低功耗", "峰值检测·卡路里·Stop模式", "60min"),
           ("11.3", "系统集成与验证", "OLED显示·算法验证·功耗测试", "30min")],
    prereqs=["第9章：I2C传感器操作", "第5章：PWM（OLED背光）"])

ext_ch11 = build_extension_page("ch11-ext", "第11章", "运动手环设计",
    mistakes=[("计步阈值设置不合理", "阈值太小误报（手部晃动计步），太大漏报；建议1.15~1.3g，结合实测调整"),
              ("Stop模式下I2C不响应", "Stop模式后需要重新初始化I2C时钟；或使用唤醒后的SystemClock_Config"),
              ("PPG信号基线漂移", "手腕佩戴松紧、光线干扰导致基线漂移；需要高通滤波去除直流分量")],
    tips=[("WHO_AM_I验证连接", "读取0x0F寄存器，LSM6DS3返回0x69；连接失败时立即报错，不要继续初始化"),
          ("DMP姿态解算", "LSM6DS3内置DMP（数字运动处理器），可直接输出欧拉角，减少MCU计算负担"),
          ("计步精度验证", "走100步后对比手机计步，误差<5步（5%）为合格；误差大调整阈值")],
    cards=[("LSM6DS3 WHO_AM_I寄存器返回值？", "0x69（地址0x0F）；返回其他值说明I2C连接失败或地址错误"),
           ("Stop模式下STM32功耗？", "约50μA（比正常工作的10~50mA低200倍），适合电池供电设备"),
           ("峰值检测为何需要300ms防抖？", "正常步频约1~3步/秒，<300ms两个峰值必为同一步的抖动")])


# ── 第12章：追光控制系统 ───────────────────────────────────────────
intro_ch12 = build_intro_page("ch12-intro", "第12章", "追光控制系统",
    objectives=[("掌握SG90舵机50Hz PWM控制方法", "应用"),
                ("设计四象限光敏传感器差分检测方案", "应用"),
                ("理解PID控制器P/I/D三个分量的作用", "理解"),
                ("完成PID参数整定并验证追光效果", "综合")],
    intro_text=("追光系统是闭环控制系统的典型应用——传感器感知误差，"
                "PID控制器计算纠正量，舵机执行调整动作。"
                "这一控制框架广泛用于太阳能板追日、工业机器人、无人机姿态控制等领域。"),
    tasks=[("12.1", "传感器与舵机基础", "四象限光敏·舵机PWM·误差计算", "60min"),
           ("12.2", "PID控制器实现", "离散PID·参数整定·防积分饱和", "60min"),
           ("12.3", "系统调参与优化", "Kp/Ki/Kd整定·死区·性能验证", "30min")],
    prereqs=["第5章：PWM舵机控制", "第7章：ADC多通道采样"])

ext_ch12 = build_extension_page("ch12-ext", "第12章", "追光控制系统",
    mistakes=[("舵机抖动（积分饱和）", "未限制积分项导致积分累积过大，加积分限幅：if(integral>100)integral=100"),
              ("四象限传感器安装不对称", "遮光板必须严格居中，否则无光时仍有偏差；用万用表测量四路电压应相等"),
              ("PID输出未限幅", "未限制output范围导致舵机超出物理限位；必须clamp到[out_min, out_max]")],
    tips=[("从纯P开始整定", "Ki=Kd=0，调Kp到刚好振荡，然后减半Kp，再加Ki消稳态误差，最后加Kd减超调"),
          ("死区设计", "误差绝对值<50（ADC单位）时不动作，防止光源正对时来回小幅抖动"),
          ("用串口实时调参", "UART接收K参数命令，运行时动态修改Kp/Ki/Kd，无需重新烧录")],
    cards=[("PID积分项的作用？", "消除稳态误差：只要误差存在，积分项就持续累加，直到输出将误差降为零"),
           ("死区的作用？", "误差很小时不动作，防止执行器持续微小抖动（系统稳定后的颤振现象）"),
           ("为何D项对噪声敏感？", "微分=误差变化率，传感器噪声导致相邻采样差异大，Kd放大后引起输出震荡")])


# ── 导出函数 ───────────────────────────────────────────────────────
def get_all_intros_extensions():
    """返回所有章节的 intro 和 extension 页面字典"""
    return {
        'intro_ch3': intro_ch3, 'ext_ch3': ext_ch3,
        'intro_ch4': intro_ch4, 'ext_ch4': ext_ch4,
        'intro_ch5': intro_ch5, 'ext_ch5': ext_ch5,
        'intro_ch6': intro_ch6, 'ext_ch6': ext_ch6,
        'intro_ch7': intro_ch7, 'ext_ch7': ext_ch7,
        'intro_ch8': intro_ch8, 'ext_ch8': ext_ch8,
        'intro_ch9': intro_ch9, 'ext_ch9': ext_ch9,
        'intro_ch10': intro_ch10, 'ext_ch10': ext_ch10,
        'intro_ch11': intro_ch11, 'ext_ch11': ext_ch11,
        'intro_ch12': intro_ch12, 'ext_ch12': ext_ch12,
    }
