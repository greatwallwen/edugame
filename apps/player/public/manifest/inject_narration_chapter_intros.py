# -*- coding: utf-8 -*-
"""
inject_narration_chapter_intros.py· C.1.1

10 个章节首页 dh 占位符（28~37 字）批量替换为完整章节引入讲解（每段 200~280 字）。
覆盖：p2-gpio-hal / ch3-intro / ch4-intro / ch5-intro / ch7-intro / ch8-intro /
     ch9-intro / ch10-intro / ch11-intro / ch12-intro

每段都用 triple-quote，避开中文里 ASCII 双引号干扰（教训 25）。
重跑幂等：直接 b['script'] = SCRIPT 全量替换。
"""
from __future__ import annotations
import os, sys
sys.path.insert(0, os.path.dirname(__file__))
from inject_narration_factory import run as run_narration  # type: ignore

# ─── p2-gpio-hal · 第 2 章 GPIO 与 HAL 库程序设计 ─────────────
P2GH_DH = """同学们好，这一节我们正式打开 STM32 编程的第一道门——GPIO 和 HAL 库。GPIO 全称 General Purpose Input/Output，通用输入输出引脚，是单片机和外部世界对话的物理通道。LED 点亮、按键检测、蜂鸣器响起、电机转动，每一个外部动作背后都是 GPIO 在控制电平的高低翻转。掌握 GPIO 你就拿到了嵌入式系统操控物理硬件的钥匙。HAL 库是 ST 官方提供的硬件抽象层，把寄存器层的复杂细节封装成一个个清晰的 C 函数——HAL_GPIO_WritePin / HAL_GPIO_ReadPin / HAL_GPIO_TogglePin 三个核心 API 你今天就能上手用，写出第一个让 LED 闪烁的程序。本节我们从 GPIO 的 8 种工作模式开始讲，再到 HAL 库的初始化序列，最后落到一个完整的代码示例。"""

# ─── ch3-intro · 第 3 章 GPIO 应用 学习目标与项目引入 ──────────
CH3_INTRO_DH = """同学们好，欢迎进入第 3 章 GPIO 应用。前面我们学完了 GPIO 的工作原理和 HAL 库基础，本章把它们用起来——做出三个真东西：LED 流水灯、按键扫描、外部中断 EXTI 实战。这三个项目是嵌入式入门的"三件套"，每一个学校的 STM32 实验课都从这里开始。但我们不止做出来，更要弄懂背后的工程细节：为什么按键要消抖？外部中断怎么配置 NVIC 优先级？流水灯怎么用最少代码做出最丰滑的效果？把这些问题钻透，你就跨过了从"跟着教程做"到"自己设计"的门槛。本章三个项目代码量不超过 200 行，但每行背后都有可以让你受益十年的工程哲学，跟我一起认真做完。"""

# ─── ch4-intro · 第 4 章 定时器应用 ─────────────────────────
CH4_INTRO_DH = """同学们好，欢迎进入第 4 章定时器应用。前面 GPIO 让我们学会让灯亮、按键响应，但要让 STM32 主动地、规律地、精确地干活——比如每秒翻一次 LED、每 100 毫秒采一次温度——就得交给定时器。定时器是 STM32 给你的"时间助手"：你只要告诉它做什么、多久一次，它就掐着表自己干，CPU 可以去处理别的任务。本章我们从定时器的核心三件套（PSC 预分频、ARR 自动重载、CNT 计数器）讲起，然后做两个实验——TIM2 中断翻转 LED + 多周期任务调度。学完后你就掌握了 STM32 时间精确控制的全部基础，下面 PWM、ADC 采样、舵机驱动通通要靠它。本章代码示范虽简单，但定时器中断的回调铁律、APB 总线倍频陷阱这些工程经验都会讲透。"""

# ─── ch5-intro · 第 5 章 PWM 输出 ────────────────────────────
CH5_INTRO_DH = """同学们好，欢迎进入第 5 章 PWM 输出。PWM——脉冲宽度调制——是嵌入式控制模拟世界的最优雅方式。LED 调光、电机调速、舵机定位、加热器功率控制，背后都是同一个原理：让数字 0/1 信号在高速切换中"等效"出一个介于 0 和满电之间的模拟量。本章我们从 PWM 的频率与占空比讲起，做出两个真项目：呼吸灯（LED 渐亮渐暗）和 SG90 舵机控制（让小舵机精确转到任意角度）。学完后你能用 STM32 驱动几乎任何"需要功率连续可调"的器件——从一颗 LED 到一台电机再到工业级电源转换器。本章是后续章节"机器视觉追光"和"运动手环"的基础，跟着实验一步一步来。"""

# ─── ch7-intro · 第 7 章 ADC 模数转换 ────────────────────────
CH7_INTRO_DH = """同学们好，欢迎进入第 7 章 ADC 模数转换。如果说前面的 GPIO 让 STM32 看见数字世界，那 ADC 就是让它看见模拟世界——光照、温度、声音、压力、电流、电压等所有连续变化的物理量。STM32 的 12 位 ADC 能把 0~3.3V 的模拟电压切成 4096 等份转成数字，0.8 毫伏精度足以读取大部分传感器。本章三个层次：先讲 ADC 的工作原理（采样保持 + 逐次逼近 SAR）；再做最简单的内部温度传感器实验；最后接上光敏电阻做实战光照采集。掌握 ADC 后你的嵌入式系统才真正长出"感官"，能感知世界并据此做决策——这是进入物联网、智能控制等高级应用的前提。"""

# ─── ch8-intro · 第 8 章 DAC 数模转换 ────────────────────────
CH8_INTRO_DH = """同学们好，欢迎进入第 8 章 DAC 数模转换。DAC 是 ADC 的反向兄弟——把 STM32 的数字结果变成真正连续的模拟输出电压。前一章 PWM 是"间接模拟"（靠 RC 滤波平均），本章 DAC 是"真正模拟"（直接输出连续电压）。STM32F1 高端型号内置 12 位 DAC，精度可达 0.8 毫伏。我们用它来做函数发生器输出正弦波/三角波/方波，做参考电压源给运放、电流源、可编程电源用。本章核心实验：先用单次写值验证 DAC 通路；再用 LUT 查找表 + DMA + 定时器三件套做出无 CPU 占用的高速波形输出。学完后你就能写出工业级数据信号链的完整代码——这是从练习题级跨到产品级的关键章节。"""

# ─── ch9-intro · 第 9 章 环境监测系统 ─────────────────────────
CH9_INTRO_DH = """同学们好，欢迎进入第 9 章环境监测系统。这是把前面所有积累综合到一起做一个真东西的章节——温度、湿度、气压、空气质量同时实时监测，OLED 屏显示，UART 上传到电脑。这套系统是现代物联网的最小单元，把千千万万这种节点连起来就是智慧农业、智慧城市、工业 4.0 的底层基础设施。本章硬件用 SHT30（温湿度）、BMP280（气压）、MQ135（气体）三颗 I2C / 模拟传感器，软件分层调度（定时器中断采样 + 主循环处理 + 显示与上传分速率运行）。学完你的简历就能多一行可信项目经验：完整设计实现多传感器 IoT 节点。"""

# ─── ch10-intro · 第 10 章 无人停车场系统 ─────────────────────
CH10_INTRO_DH = """同学们好，欢迎进入第 10 章无人停车场系统。这章我们做一个真正能用的智能控制项目：超声波感知车辆 + 舵机自动开闸 + OLED 屏显示空位 + UART 计费上传。它把前面所有学过的东西全用上——HC-SR04 超声测距、SG90 舵机驱动、状态机、I2C 显示、串口通信，组合成一台能在小区门口实际跑的迷你系统。本章核心是有限状态机思维：把一个复杂业务（IDLE → DETECT → OPEN → WAIT_PASS → CLOSE → IDLE）拆解成清晰的状态和转移条件——这种思维方法学会后任何控制系统你都能设计。这是从写练习题到写产品的最大思维跃迁。"""

# ─── ch11-intro · 第 11 章 运动手环设计 ──────────────────────
CH11_INTRO_DH = """同学们好，欢迎进入第 11 章运动手环设计。我们把 STM32 + MPU6050 6 轴 IMU + OLED 屏组合起来，做出一个真正能戴在手腕上的智能手环。30 毫米见方的板子上集成 I2C 总线、加速度采集、陀螺仪、计步算法、低功耗管理、屏幕刷新——这就是消费电子产品的最小可工作原型。本章重点学三件事：第一是 MEMS 传感器的工作原理（微米级机械结构 + 电容感应）；第二是峰值检测计步算法（学生时代写到准确率 95% 已达商业级）；第三是 STM32 三种低功耗模式的协同（让 200mAh 电池跑出 7 天续航）。学完你拿着这块手环面试，比同期同学多一个"完整产品"作品。"""

# ─── ch12-intro · 第 12 章 追光控制系统 ──────────────────────
CH12_INTRO_DH = """同学们好，欢迎进入第 12 章追光控制系统——本课的综合大作业。我们用 4 颗光敏电阻组成十字传感器阵列、双轴舵机控制面板朝向、PID 算法实时让面板正对最强光源——这就是太阳能板自动追日的最小工作原型。这个项目把 ADC 多通道扫描、DMA、PWM 舵机、PID 闭环、I2C 显示六大技术合一，是 STM32 课程的最高综合演练。但更重要的是 PID 思维——这五行代码的反馈控制器是无人机姿态、CNC 刀位、激光跟踪、空调温控、ABS 防抱死所有自动化系统的共同祖先。学完这个项目你拿到的不是一台追光器，是整个工业控制学科的入场券。"""


def _build_factory_dict():
    """构造 narration_factory 期望的 {pid: {bid: {kind, script}}} 嵌套字典"""
    pages = {
        'p2-gpio-hal':  ('p2gh-dh', P2GH_DH),
        'ch3-intro':    ('ch3-intro-dh', CH3_INTRO_DH),
        'ch4-intro':    ('ch4-intro-dh', CH4_INTRO_DH),
        'ch5-intro':    ('ch5-intro-dh', CH5_INTRO_DH),
        'ch7-intro':    ('ch7-intro-dh', CH7_INTRO_DH),
        'ch8-intro':    ('ch8-intro-dh', CH8_INTRO_DH),
        'ch9-intro':    ('ch9-intro-dh', CH9_INTRO_DH),
        'ch10-intro':   ('ch10-intro-dh', CH10_INTRO_DH),
        'ch11-intro':   ('ch11-intro-dh', CH11_INTRO_DH),
        'ch12-intro':   ('ch12-intro-dh', CH12_INTRO_DH),
    }
    out = {}
    for pid, (bid, script) in pages.items():
        out[pid] = {bid: {'kind': 'digital-human', 'script': script}}
    return out


if __name__ == '__main__':
    run_narration(_build_factory_dict(), expected_pages=10)
