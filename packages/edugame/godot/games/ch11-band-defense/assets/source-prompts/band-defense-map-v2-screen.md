# Band Defense Background Map v2 Screen

Generated for `ch11-band-defense` as a revision of `band-defense-map-v1.png`.

Workspace asset:

```text
packages/edugame/assets/games/ch11-band-defense/backgrounds/band-defense-map-v2-screen.png
```

Runtime path from the Godot project:

```text
res://assets/backgrounds/band-defense-map-v2-screen.png
```

Final project image size:

```text
1280x720
```

## Prompt

```text
Edit the provided tower-defense background map image.

Use case: precise-object-edit
Asset type: 1280x720 Godot tower-defense background map variant.
Primary request: Keep the existing left gameplay map, cyan route, four empty tower pads, sensor input node, trusted-data core, PCB traces, and overall composition unchanged as much as possible. Modify only the right-side dark HUD panel so it feels like an active embedded device screen rather than a flat empty panel.
Right-side screen treatment: Turn the right panel into a glassy powered-on display with subtle blue-green inner glow, faint scanlines, soft reflection, thin luminous bezel, layered translucent UI zones, small empty graph frames, waveform ghosts, matrix-like diagnostic grid, and a few abstract circular status widgets. It should read clearly as a screen, but it must still leave plenty of usable negative space for actual game UI drawn later.
Style continuity: Match the current light sci-fi PCB / smart-band data-link style. Keep the right screen restrained, professional, and not cyberpunk-dark. Use cyan and green accents consistent with the data path.
Text: no readable text, no letters, no numbers, no labels, no watermark.
Constraints: Do not add enemies, towers, characters, people, logos, checkmarks, app badges, weapons, or solar-tracking imagery. Do not move the path or tower pads. Do not change the left-side gameplay field except for tiny lighting harmonization if necessary.
```

## Notes

- Generated with the built-in image editing path using the repo default `image2` preference.
- The generated source was copied from Codex's generated-image directory into the central `edugame` asset library.
- The workspace copy was resized to 1280x720 for the current Godot canvas.
